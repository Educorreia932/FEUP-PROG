#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int n;
	double q, j, A;

	cout << "Inser the number of years, the initial amount and the interest rate (in percentage): ";	

	A = q * pow(((1 + (j / 100)) / n), n); //Correct formula?

	cout << "The final amount is " << A << ".";

	return 0;
}